OPTION(USE_FreeImage "Build with FreeImage" ON)
IF(USE_FreeImage)
  FIND_PACKAGE(FreeImage REQUIRED)
  IF(FreeImage_FOUND)
    INCLUDE_DIRECTORIES(${FreeImage_INCLUDE_DIRS})
    ADD_DEFINITIONS(-DBUILD_WITH_FREEIMAGE)
  ENDIF()
ENDIF()
